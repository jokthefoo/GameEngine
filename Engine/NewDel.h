#pragma once
/*
enum Alignment
{
	ALIGN_DEFAULT,
	ALIGN_16 = 16,
	ALIGN_32 = 32
};

void * operator new(size_t size);
void * operator new(size_t size, Alignment i_align);
void * operator new[](size_t size);

void operator delete(void * i_ptr);
void operator delete(void * i_ptr, Alignment i_align);
void operator delete[](void * i_ptr);*/